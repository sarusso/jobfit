
# Note: if setting up Let's Encrypt, use these certificates for the 
# intermediate step, according to the REANDE:
# SSLCertificateFile /root/certificates/selfsigned.crt
# SSLCertificateKeyFile /root/certificates/selfsigned.key
# SSLCACertificateFile /root/certificates/selfsigned.ca-bundle


#------------------------
# Force https
#------------------------
<VirtualHost *:80>

    ServerAdmin admin@jobfit.app
    RewriteEngine On
    RewriteCond %{HTTPS} off
    RewriteRule (.*) https://%{HTTP_HOST}%{REQUEST_URI}
    AllowEncodedSlashes NoDecode
       
</VirtualHost>


#------------------------
# Catch-all
#------------------------
<VirtualHost *:443>
    ServerAdmin admin@jobfit.app
    SSLEngine on
    SSLCertificateFile /etc/letsencrypt/live/__JOBFIT_HOST__/cert.pem
    SSLCertificateKeyFile /etc/letsencrypt/live/__JOBFIT_HOST__/privkey.pem
    SSLCACertificateFile /etc/letsencrypt/live/__JOBFIT_HOST__/fullchain.pem
    DocumentRoot /var/www/html
</VirtualHost>


#------------------------
#  JobFit Web App
#------------------------
<VirtualHost *:443>
    ServerAdmin admin@jobfit.app
    ServerName __JOBFIT_HOST__
    ProxyPass / http://webapp:8080/
    ProxyPassReverse / http://webapp:8080/
    AllowEncodedSlashes NoDecode

    SSLEngine on
    SSLCertificateFile /etc/letsencrypt/live/__JOBFIT_HOST__/cert.pem
    SSLCertificateKeyFile /etc/letsencrypt/live/__JOBFIT_HOST__/privkey.pem
    SSLCACertificateFile /etc/letsencrypt/live/__JOBFIT_HOST__/fullchain.pem

    # Browser-specific fixes
    BrowserMatch "MSIE [2-6]" \
            nokeepalive ssl-unclean-shutdown \
            downgrade-1.0 force-response-1.0
    BrowserMatch "MSIE [17-9]" ssl-unclean-shutdown

    # Required for the Open ID connect redirects to work properly
    RequestHeader set X-Forwarded-Proto 'https' env=HTTPS        

</VirtualHost>

